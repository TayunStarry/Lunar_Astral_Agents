# 05 独立 AI 引擎与运维工具

> [🏠 文档地图](README.md) | [◀ 上一章](04-公共子系统.md) | [下一章 ▶](06-前端资源库.md)

---

## 5.1 agent_search —— 智能网络检索

`subsystem/agent_search/`。AI 驱动的网络搜索智能体，基于 Chromedp 控制 Edge/Chrome 浏览器（结合多模态 LLM 与嵌入模型）：多引擎搜索 → 页面提取 → AI 摘要 → 相关性判定 → 报告生成 → 向量记忆存储。

### 统一搜索流程

**单一模式**（不区分快速/深度双模式）。`executeSearchPipeline` 串联以下步骤：

| 步骤 | 说明 |
|------|------|
| 1 实体与关键词提取 | AI 从查询提取核心完整实体（独立专名）与关键词数组；失败回退 `cleanSearchText` 规则清洗 |
| 2 初始查询拼接 | 关键词去重后以空格拼接为初始查询语句 |
| 3 向量嵌入 | 对初始查询调用嵌入模型得到查询向量，用于摘要相关性打分 |
| 4 记忆库检索 | 高相似度（`MemoryDirectAnswerMin`）且非失败记录则直接复用；时效性需求或缺相似度则走网络 |
| 5 网络搜索 | 每轮 `ExecuteSearch` 多引擎降级链（Bing→百度→搜狗），取 TopN（`SingleSearchResults`） |
| 6 标题初筛 | 用核心实体过滤无关标题（分隔符归一化 + token 化匹配） |
| 7 逐页混合提取 | 文本优先，文本不足（`TextHeavyThreshold`）转滚动截图 → AI 摘要 → 嵌入打分 + 实体命中判定 |
| 8 综合判定 | 有效摘要拼接后 AI 一次性判定能否解答（`judgeComprehensive`），可解答则生成报告并入记忆库 |
| 9 增强搜索 | 无法解答时 `enhanceSearchText` 推测真实意图换词再跑一轮（attempt=2） |
| 10 兜底返回 | 仍失败返回「月华不知道」并记录失败经验入记忆库（`phaseUnknownAnswer`） |

**关键设计**：
- 相关性判定不过度保守——页面能部分解答即视为有效，避免「有答案却说不知道」。
- 失败/低质量记录不作答案复用，仅作下次搜索避坑经验（`isFailedMemoryContent` / `isLowQualityAnswer`）。
- 字典网站过滤（`filterDictionarySites`）+ Bing 工具站兜底检测（`detectSearchFallback`），节省浏览器与 token。
- 单页超 10 秒（`PageFastSkipTimeout`）直接跳过，不反复重启浏览器；浏览器重启仅用于资源异常（内存/CPU 超限）或引擎页故障。

### 关键文件与函数

| 文件 | 关键函数 | 职责 |
|------|------|------|
| `agent.go` | `InitSearch` / `Search` / `executeSearchPipeline` / `phaseNetworkSearch` / `searchTextAndJudge` / `filterByTitleEntities` / `isSummaryValid` / `detectSearchFallback` / `enhanceSearchText` / `phaseMemoryLookup` / `phaseUnknownAnswer` / `ensureBrowserHealthy` | 主控编排与统一搜索流程 |
| `ai.go` | `callAI` / `callChatAPI` / `callEmbedding` / `extractKeywordsAndEntities` / `summarizeContent` / `summarizeVisualContent` / `judgeMemory` / `judgeComprehensive` / `enhanceSearchText` / `generateReport` | OpenAI 兼容 API 调用与各 AI 决策 |
| `browser.go` | `LaunchBrowser` / `CloseBrowser` / `ExecuteSearch` / `searchOnEngine` / `parseBingResults` / `parseBaiduResults` / `parseSogouResults` / `ExtractPageContent` / `cleanDOMText` | Chromedp 浏览器控制、多引擎搜索与页面解析 |
| `memory.go` | `initMemoryCollection` / `lookupMemory` / `storeMemoryRecord` / `formatMemoryRecord` | v2 标签向量中介检索记忆读写（经 FileManager module） |
| `monitor.go` | `monitorBrowserHealth` / `isCPUSustainedHigh` / `trackCPUHigh` / `ResetCPUTracking` | 浏览器健康监控（gopsutil） |
| `screenshot.go` | `CapturePageScreenshots` / `CaptureFullPageScreenshot` | 分页滚动截图（`MaxScreenshotsPerPage` 帧） |
| `config.go` | `DefaultSearchConfig` / `ValidateSearchConfig` | 搜索配置 |

### 关键流程常量（variable.go）

| 常量 | 默认值 | 说明 |
|------|--------|------|
| `SingleSearchResults` | 10 | 每轮进入搜索引擎并提取的 TopN 链接 |
| `MaxScreenshotsPerPage` | 6 | 单页最大截图数（逐屏滚动） |
| `TextHeavyThreshold` | 500 | 文本密集型判定阈值（字符数） |
| `EmbedRelevanceThreshold` | 0.5 | 摘要与查询嵌入余弦相似度阈值（≥视为相关） |
| `MemorySimilarityMin` | 0.55 | 记忆库相似度准入阈值 |
| `MemoryDirectAnswerMin` | 0.70 | 直接复用记忆答案的最低相似度 |
| `MaxContextTokensDefault` | 16384 | 单次 AI 调用最大上下文 tokens |
| `PageFastSkipTimeout` | 10s | 单页提取超时，超时直接跳过 |
| `BrowserMaxMemMB` / `BrowserMaxCPUPercent` | 4096 / 80 | 浏览器资源上限，超标触发重启 |

### 配置说明

模型配置从 `lunar_config.json` 的 `general_config` 模块读取（`GeneralConfig`）：

| 配置项 | 说明 |
|--------|------|
| `SearchMultimodalURL` | 多模态模型 API 地址 |
| `SearchMultimodalModel` | 多模态模型名称 |
| `SearchMultimodalKey` | 多模态模型 API 密钥 |
| `SearchEmbeddingURL` | 嵌入模型 API 地址 |
| `SearchEmbeddingModel` | 嵌入模型名称 |
| `SearchEmbeddingKey` | 嵌入模型 API 密钥 |

### 依赖

- 本地：`FileManager`、`GeneralConfig`、`LoggerGeneral`
- 外部：`chromedp v0.16.0`（浏览器）、`shirou/gopsutil/v3`（进程监控）、`golang.org/x/net`、`mattn/go-sqlite3`（经 FileManager 的间接依赖）
- 测试入口：`cmd/search_test/`、`cmd/browser_test/`

### 使用方式

模型配置从 `lunar_config.json` 的 `general_config` 模块读取（搜索多模态模型、嵌入模型的 URL/模型名/API Key）。

**命令行测试**：

```powershell
cd d:\Lunar_Astral_Agents\subsystem\agent_search\cmd\search_test
go run .
```

**作为 Go 库集成**：

```go
import AgentSearch "LunarSubsystem/AgentSearch"

// 1. 初始化（必须先于 Search）
cfg := AgentSearch.DefaultSearchConfig()
cfg.MaxContextTokens = 16384
if err := AgentSearch.InitSearch(cfg); err != nil {
    // 处理初始化失败
}

// 2. 执行搜索（阻塞、串行）
report, err := AgentSearch.Search("查询南京南站是哪里，有哪些便捷的交通接驳")
// report.Answer       — AI 生成的答案
// report.UsedSources  — 引用来源 URL
// report.FromMemory   — 是否直接来自记忆库
// report.SearchRounds — 实际搜索轮次
```

**关键配置项**：

| 配置项 | 默认值 | 说明 |
|--------|--------|------|
| `MaxContextTokens` | 16384 | 单次 AI 调用最大上下文 tokens |
| `MemoryDBDir` | `local_data/database/memory` | 向量记忆库存储目录 |

---

## 5.2 qwen3_tts —— 语音合成

`subsystem/qwen3_tts/`。全本地化 Qwen3-TTS 引擎，三层异构架构：**Go HTTP 服务层 → C++ 推理引擎（DLL）→ GGML 张量计算库（多 GPU 后端）**。支持音色克隆（参考音频）与 LRU + singleflight 音频缓存。

### 推理管线

```
text_tokenizer (GPT-2 BPE 分词)
  → tts_transformer (Encoder + Cross-Attention)
  → audio_tokenizer_decoder (latent → PCM float32)
  → WAV 封装
```

### 关键文件与函数（Go 层）

| 文件 | 关键函数 | 职责 |
|------|------|------|
| `main.go` | — | 入口：读 `GeneralConfig` 端口/模型目录/参考音频，`InitTTSEngine` + `startServer` |
| `server.go` | `registerHandlers` / `startServer` / `waitForServerReady` | 注册 `/tts`、`/upload/`、`/health`，优雅关闭 |
| `module/generate.go` | `InitTTSEngine` / `SynthesizeText` / `EncodePCMToWAV` / `getOrExtractEmbedding` / `synthesizeWithCache` / `TTSHandler` | CGO 绑定 + TTS 合成 |
| `module/cache.go` | `NewTTSCache` / `ComputeCacheKey` / `GetOrCreateInflight` | LRU 缓存 + singleflight 防击穿 |
| `module/variable.go` | `ttsCache`（容量 64）/ `embedCache` | 全局状态 |
| `module/type.go` | `TTSCache` / `InflightCall` / `TTSEngine` | 类型定义 |

### C++ 引擎（cpp/src/）

- `qwen3tts_c_api.cpp/h`：C API 层，导出 `qwen3_tts_create`、`qwen3_tts_synthesize_with_embedding`、`qwen3_tts_extract_embedding_file`、`qwen3_tts_synthesize_streaming*` 等
- `tts_transformer` / `audio_tokenizer_encoder|decoder` / `gguf_loader` / `text_tokenizer`
- `cpp/ggml/`：完整 GGML 库（CUDA/Vulkan/Metal/SYCL/OpenCL/BLAS 多后端）

### 端

默认端口 `36789`（即 `-basic-port` 默认值，可加 `-basic-port <端口>` 修改；与月华 llama-proxy 同端口，TTS 作为同一服务暴露 `/tts`）。

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/tts` | 文本转语音生成（JSON） |
| POST | `/upload/` | 参考音频上传（`multipart/form-data`，字段 `audio`，支持 WAV） |
| GET | `/health` | 健康检查 |

### TTS 合成请求

`POST /tts/`（JSON，除 `text` 外均为可选）：

```json
{
  "text": "你好，我是月华，很高兴认识你！",
  "ref_audio": "",              // 参考音频路径，为空则使用默认音色
  "language_id": 2055,          // 语言ID（2055=中文，默认2055）
  "temperature": 0.8,           // 生成随机性
  "top_k": 0,                   // Top-K 采样（0=不限制）
  "top_p": 0.9,                 // Top-P 采样
  "max_tokens": 0,              // 最大生成 token 数（0=不限制）
  "repetition_penalty": 1.1,    // 重复惩罚
  "threads": 0,                 // 线程数（0=自动）
  "disable_cache": false        // false=优先用缓存，true=强制重新合成
}
```

### 缓存机制

`module/cache.go` 实现 **LRU + singleflight** 音频缓存，`module/variable.go` 中的 `ttsCache` 为全局实例：

- **LRU 容量**：上限 64 条，淘汰最久未使用条目，`sync.RWMutex` 保护并发
- **缓存键**：基于文本、参考音频、语言 ID、采样参数的 **SHA256 哈希**（`ComputeCacheKey`）
- **disable_cache 语义**：`false`（默认）优先查询缓存，命中返回、未命中合成并缓存；`true` 跳过缓存查询强制重新合成，合成后仍更新缓存（`synthesizeWithCache`）
- **防击穿**：`GetOrCreateInflight` 单飞——并发相同请求只合成一次，其余等待共享结果
- **Embedding 缓存**：`embedCache` 复用音色克隆的参考音频 embedding（`getOrExtractEmbedding`）

### 构建

`build.ps1` 3 阶段：Stage 1 `build_ggml.ps1`（编 GGML）→ Stage 2 `build_cpp.ps1`（编 C++ DLL，默认 `EnableVulkan=true`）→ Stage 3 `CGO_ENABLED=1` 编 Go。产物 `Qwen3_TTS_Lunar.exe` + `qwen3tts.dll`。可 `-SkipGGML/-SkipCPP/-SkipGo/-Clean` 分段，`-BuildType Debug` 以调试模式编译。

### 编译与运行

```powershell
cd d:\Lunar_Astral_Agents\subsystem\qwen3_tts
.\build.ps1            # 可 -SkipGGML/-SkipCPP/-SkipGo/-Clean
.\Qwen3_TTS_Lunar.exe
```

运行要求：① 模型文件（GGUF 格式）放置在 `{LocalDir}/models/` 目录；② 参考音频文件存在（默认 `{LocalDir}/audios/lunar-template.wav`）。程序启动后作为后台 HTTP 服务运行（默认端口 `-basic-port` 可改）。

### 常见问题

- **模型文件在哪里下载？** Qwen3-TTS 的 GGUF 格式模型可从 Hugging Face 或 ModelScope 获取，将 `.gguf` 文件放入 `{LocalDir}/models/` 即可。
- **如何获得更好的音色克隆效果？** 使用高质量的参考音频（建议 16kHz 或 24kHz，单声道，PCM WAV 格式），时长建议 5-15 秒，内容涵盖丰富的语音特征。
- **CUDA 加速如何启用？** 在编译 GGML 时启用 CUDA 支持，确保系统已安装 CUDA Toolkit 12.x 或 13.x。编译完成后引擎会自动检测并使用 GPU。
- **生成速度慢怎么办？** ① 启用 GPU 加速（CUDA/Vulkan）；② 减少单次生成长度；③ 使用量化模型（Q4_K_M 等）减少推理计算量。

---

## 5.3 qwen_asr —— 语音识别

`subsystem/qwen_asr/`。全本地化 Qwen3-ASR 引擎，**纯 C 推理引擎 + Go HTTP 服务 + WebView 前端**。支持 30 种语言、0.6B/1.7B 双模型自动检测、BF16 零拷贝权重、SIMD 加速（AVX2/AVX-512/NEON）。

### 推理管线

```
qwen_load_wav → qwen_mel_spectrogram (STFT + Mel filterbank)
  → qwen_encoder_forward (Conv2D Stem + 窗口注意力 Transformer)
  → 构建 ChatML prompt → qwen_asr_decoder 自回归解码 → BPE 分词器输出文本
```

### 关键文件与函数

| 文件 | 职责 |
|------|------|
| `main.go` | 入口：`New()` 初始化引擎、`NewAsrHandler()`、HTTP + BrowserClient WebView（648×960） |
| `asr.go` | CGO 桥接：`New`（`qwen_load`）/ `Close`（`qwen_free`）/ `TranscribeWavFile` / `TranscribeAudioBuffer` / `SetLanguage` / `SetPrompt` / `SupportedLanguages` |
| `handler.go` | `/asr`（32MB 上限，非 WAV 用 FFmpeg 转 16kHz/16bit/mono）、`/health`、`estimateConfidence` |
| C 引擎 | `qwen_asr.h/c`（`qwen_load/transcribe/transcribe_audio`）、`qwen_asr_audio.c`、`qwen_asr_encoder.c`、`qwen_asr_decoder.c`、`qwen_asr_tokenizer.h/c`、`qwen_asr_safetensors.h/c`（多分片 + mmap） |
| 内核 | `qwen_asr_kernels.c` + SIMD 分派（`_avx.c` / `_neon.c` / `_generic.c`）：`qwen_bf16_matvec_fused`、`qwen_argmax_bf16_range`、`qwen_causal_attention` 等 |

### 端点

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/asr` | 音频转写（`multipart/form-data`，字段 `audio`） |
| GET | `/health` | 健康检查 |

### 转写请求约束

`POST /asr` 表单字段 `audio` 提交音频文件：

- **最大文件大小**：32MB
- **支持格式**：WAV（推荐）或任意 FFmpeg 可转码格式
- **非 WAV 自动转码**：FFmpeg 转为 **16kHz / 16-bit / mono / PCM WAV** 后送入引擎

### 转写响应

```json
{
  "status": "success",
  "text": "你好，我是月华，很高兴认识你",
  "confidence": 0.95,
  "audio_format": "wav"
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| `status` | `string` | `"success"` 或 `"error"` |
| `text` | `string` | 转写文本 |
| `confidence` | `float` | 置信度估计（0.0-1.0，`estimateConfidence` 计算） |
| `audio_format` | `string` | 实际处理的音频格式 |

### 运行时环境变量

| 环境变量 | 说明 |
|----------|------|
| `PORT` | HTTP 服务端口 |
| `QWEN_VERBOSE` | 是否输出详细日志 |
| `QWEN_BF16_CACHE_MB` | BF16 权重缓存内存上限（MB） |

### 依赖 / 构建

- 本地：`BrowserClient`、`GeneralConfig`、`LoggerGeneral`；外部：`webview_go`、OpenBLAS（可选 `-DUSE_BLAS -fopenmp`）
- `build.ps1`：`CGO_ENABLED=1 -O3 -march=native -ffast-math` → `Qwen_ASR_Lunar.exe`

### 编译与运行

```powershell
cd d:\Lunar_Astral_Agents\subsystem\qwen_asr
.\build.ps1            # 可选 OpenBLAS 加速
.\Qwen_ASR_Lunar.exe
```

程序自动打开 WebView 窗口（648×960），提供文件上传、浏览器录音与转写结果展示。

### 常见问题

- **支持哪些音频格式？** 推荐使用 **16kHz, 16-bit, mono, PCM WAV** 格式以获得最佳效果。其他格式（MP3、WebM、OGG、FLAC 等）会自动通过 FFmpeg 转换为标准格式。
- **如何启用 OpenBLAS 加速？** 编译时启用 OpenBLAS 标志并在链接路径中放置 OpenBLAS 库文件即可。
- **0.6B 和 1.7B 模型如何选择？** 引擎会自动检测模型目录中的 SafeTensors 文件，根据模型规模自动判断。0.6B 速度更快，1.7B 精度更高。
- **为什么转写速度慢？** ① 启用 OpenBLAS 加速（编译时）；② 使用 0.6B 模型替代 1.7B；③ 检查 CPU 是否有 AVX2 指令集支持；④ 长音频使用分段模式。

---

## 5.4 environment_repair —— 运维工具箱

`subsystem/environment_repair/`。交互式终端菜单（1~5），四项功能。

### 启动菜单

启动 `Environment_Repair.exe` 后按数字键选择功能：

```
[1] 资源补全修复   从内嵌资源释放缺失的 local_data 文件（仅补缺失、不覆盖）
[2] 端口占用释放   扫描并终止占用端口的进程，自动验证释放结果
[3] HTTPS 代理服务 启动 HTTPS → HTTP 反向代理（自动生成 TLS 证书）
[4] 分卷打包归档   按配置打包为 7z 分卷压缩包
[5] 退出
```

### 功能与关键文件

| 功能 | 文件 / 函数 | 说明 |
|------|------|------|
| ① 资源补全修复 | `repair.go` `EnsureLocalData()` | `//go:embed embedded_data/*` 释放 audios/images/package 到 local_data，仅补缺失不覆盖 |
| ② 端口占用释放 | `release.go` `ExecutePortRelease()` 等 | PowerShell `Get-NetTCPConnection` 优先，回退 `netstat`；`taskkill /f` 终止；`verifyPorts` 验证 |
| ③ HTTPS 代理 | `proxy.go` `RunHTTPSProxy()` / `handleProxyRequest()` / `handleWebSocketProxy()` | HTTP 用 `httputil.NewSingleHostReverseProxy`；WS 走 TCP 隧道双向转发；自签名 RSA-2048 证书（含 SAN）持久化复用 + `corsMiddleware` |
| ④ 分卷打包归档 | `package.go` + `component/` | 列出 JSON 配置 → 加载 → 确认 → 7z 分卷 |

### component/ 子模块（分卷打包）

| 文件 | 职责 |
|------|------|
| `config.go` / `config_loader.go` | 打包配置结构与加载（`ArchiveConfig`：Import/Exclude/Archive/Output/Size/Level；排除规则支持 `*.ext`/`node_modules`) |
| `execute.go` / `create.go` | 主流程 + `createVolume`（7z -v{size}m -mx{level}） |
| `check.go` / `clean.go` | `find7zPath` 检测 / `cleanOldParts` 清理旧分卷 |
| `progress.go` | 解析 7z 输出渲染彩色进度条 |
| `source.go` / `utils.go` | 源路径处理 |

### 依赖 / 构建

- 依赖：`GeneralConfig`；外部工具：**7z（必需，分卷打包功能需此工具）**、rsrc、powershell/netstat/taskkill
- **TLS 证书**：自签名 RSA-2048（含 SAN），`RunHTTPSProxy` 自动生成并**持久化到 `GeneralConfig.CertFile` / `KeyFile` 指定路径，二次启动直接复用**；`corsMiddleware` 处理跨域
- `build.ps1`：先 `Sync-EmbeddedData`（从 local_data 同步到 `embedded_data/`，确保 go:embed 生效），`CGO_ENABLED=1`，产物 `Environment_Repair.exe`（CLI 不隐藏控制台）

### 编译与运行

```powershell
cd d:\Lunar_Astral_Agents\subsystem\environment_repair
.\build.ps1
.\Environment_Repair.exe
```

启动后按菜单选择功能（1~5，见上表）。依赖：`GeneralConfig`；外部工具 **7z**（仅分卷打包功能需要）；TLS 证书自动生成（自签名）并持久化到 `GeneralConfig.CertFile` / `KeyFile` 指定路径，二次启动复用。