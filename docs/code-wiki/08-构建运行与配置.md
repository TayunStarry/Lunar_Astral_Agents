# 08 构建、运行与配置

> [🏠 文档地图](README.md) | [◀ 上一章](07-依赖关系.md)
> 相关源文档：根 [README](../../README.md) · [lunar_config.json](../../local_data/lunar_config.json) · 引擎 CLI 参数参考见 [10](10-llama.cpp-参数参考.md) / [11](11-stable-diffusion.cpp-参数参考.md)

---

## 1. 环境要求

- **运行时**：Windows 10 21H2+（64 位），WebView2 Runtime（Win11 预装），可选 NVIDIA CUDA / FFmpeg / Vulkan。
- **构建**：Go 1.24+、Node.js + npm（TypeScript 编译）、GCC（MinGW-w64/TDM-GCC，CGO 模块）、rsrc（`go install github.com/akavel/rsrc@latest`）、CMake（qwen3_tts C++ 阶段）。

---

## 2. 一键构建

```powershell
# 统一构建脚本（位于 subsystem/，构建全部主要模块）
cd d:\Lunar_Astral_Agents\subsystem
.\build.ps1                # 可选 -TargetOS windows|linux|darwin -TargetArch amd64|arm64 -WithGo
```

`build.ps1` 依次构建：`qwen3_tts`（默认仅 C++ 库，`-WithGo` 时含 Go 程序）→ `environment_repair` → `lunar_astral` → `crystal_astral` → `ltp9_keygen`。共享的 Go 探测逻辑位于 `build_common.ps1`（`Find-Go` / `Resolve-Go`）。

---

## 3. 单独编译

```powershell
# 核心系统——月华（含前端 TypeScript 编译）
cd d:\Lunar_Astral_Agents\lunar_astral
.\build.ps1

# 扩展系统——琉璃（CGO：内置 qwen_asr C 引擎 + OpenBLAS，需 GCC）
cd d:\Lunar_Astral_Agents\crystal_astral
.\build.ps1

# 语音合成（3 阶段：GGML → C++ DLL → Go；默认仅前两阶段，-WithGo 编 Go）
cd d:\Lunar_Astral_Agents\subsystem\qwen3_tts
.\build.ps1            # 可 -SkipGGML/-SkipCPP/-SkipGo/-Clean -WithGo

# 语音识别（独立程序形态）
cd d:\Lunar_Astral_Agents\subsystem\qwen_asr
.\build.ps1

# 运维工具箱（自动同步 embedded_data）
cd d:\Lunar_Astral_Agents\subsystem\environment_repair
.\build.ps1

# LTP3 / LTP9 权限密钥生成器
cd d:\Lunar_Astral_Agents\subsystem\ltp3_keygen   # 或 ltp9_keygen
.\build.ps1
```

各 `build.ps1` 均为自包含脚本，内部自动处理前置步骤（rsrc 图标、TS 编译、CGO 参数等）。构建脚本支持 `-TargetOS`（windows/linux/darwin）× `-TargetArch`（amd64/arm64）交叉编译参数；运行目标平台为 Windows。

---

## 4. 编译产物

产物统一输出到项目根 `d:\Lunar_Astral_Agents\`：

| 文件 | 系统 | 说明 |
|------|------|------|
| `Lunar_Astral.exe` | 钛宇-月华 | AI 桌面智能体主程序 |
| `Crystal_Astral.exe` | 钛宇-琉璃 | 工具集扩展程序（含 LTP3/LTP9 引擎、内置语音引擎） |
| `Qwen_ASR_Lunar.exe` | 语音识别 | 独立语音识别程序 |
| `Qwen3_TTS_Lunar.exe` | 语音合成 | 独立语音合成程序（`-WithGo` 构建） |
| `Environment_Repair.exe` | 运维工具 | 交互式终端菜单 CLI |
| `LTP3Keygen.exe` / `LTP9Keygen.exe` | 密钥工具 | LTP3/LTP9 权限密钥生成器 |

补充：`qwen3_tts` 额外产出 `qwen3tts.dll`（C++ 推理引擎）；`lunar_astral` 的前端产物 `agentSystem.js` 经 Go embed 内嵌于可执行文件。

---

## 5. 运行模式（月华）

```powershell
.\Lunar_Astral.exe              # 直接运行
.\Lunar_Astral.exe -developer   # 开发模式（日志不限流）
.\Lunar_Astral.exe -basic-port 36800   # 指定基础端口
```

月华默认端口 `36789`；llama-server 使用 `ModelPort`（36790）；琉璃固定端点 `EnginePort`（36792）。

---

## 6. 双层配置架构

1. **命令行参数（flag）** —— 第一优先级（如 `-basic-port`、`-developer`）
2. **`local_data/lunar_config.json`** —— 按字段覆盖默认值（`server.developer` / `server.allow_diffusion` 例外：文件存在时以文件值为准）

配置结构详见 [04-公共子系统 §4.1](04-公共子系统.md)。

### lunar_config.json 关键分组

| 分组 | 字段 | 说明 |
|------|------|------|
| `models` | asr_model / diffusion_model / variational_model / prompt_analysis_model / real_esrgan_model | AI 模型文件路径 |
| `server` | developer / allow_diffusion / bridging_type / bridging_path / bridging_token / bridging_users / bridging_group_keywords / bridging_group_trigger_probability | 开发开关与 NapCat QQ 桥接 |
| `agent` / `memory` / `search` | embedding_model/url/key、multimodal_model/url/key | 各域模型分流（默认走 `http://127.0.0.1:36789/v1`） |
| `web_search` | save_screenshots / screenshot_dir / max_pages / max_results / results_scroll_captures / page_scroll_captures / summary_max_chars / max_run_seconds | 琉璃内置 Web-LTP 网络搜索智能体（见 [09 §5.4](09-LTPX协议-月华工具包.md)）：截图是否落盘与目录、进入结果页数上限（≤10）、结果页/单页滚动截图张数、单页摘要硬切断字符数（≤4096）、单次运行总时长软上限；每次运行热读取，字段缺失用默认值 |

### 模型预设文件（models.ini）

`local_data/models/models.ini` 为 llama.cpp `--models-preset` 格式的多模型预设，由月华启动 llama-server 时传入。每个段即一个模型服务：

| 段 | 说明 |
|----|------|
| `[system-multimodal]` | 多模态模型（`model` / `mmproj`，可含 `ctx-size`、推测解码 `spec-type` / `model-draft` 等参数） |
| `[system-embedding]` | 嵌入模型（`model`，`embeddings = true`） |

段名与 `general_config` 的默认模型名（`system-multimodal` / `system-embedding`）对应。

---

## 7. 添加 / 切换模型

1. 将 GGUF 模型放入 `local_data/models/`。
2. 语言/嵌入模型：编辑 `local_data/models/models.ini` 对应段路径。
3. ASR/扩散等辅助模型：编辑 `lunar_config.json` 的 `models` 分组。
4. 重启程序生效。

---

## 8. 独立识别引擎补充参数

Qwen_ASR 运行时环境变量（可选）：`PORT`（默认 36791）、`QWEN_VERBOSE`、`QWEN_BF16_CACHE_MB`（默认 1024）。
