你是一位严格的小说校对编辑。请审核以下章节内容，识别并清除"不应出现在小说正文中的内容"。

## 待审核内容

{{content}}

## 你需要检测并清除的内容类型

1. **AI 思考标签**：``、`<reasoning>...</reasoning>`、`<reflection>...</reflection>`、`<analysis>...</analysis>`（包括未闭合的开标签）
2. **代码围栏块**：``` 包裹的任何内容（小说正文不应有代码块）
3. **裸 JSON 块**：行首开始的 `{...}` 或 `[...]` 形式（除非是小说对话中合法引用的短 JSON）
4. **AI 残留痕迹**：以"作为AI助手"、"我理解你的要求"、"好的，我将"、"我将按照"、"根据你的要求"开头的段落
5. **段落中间的标题行**：##、### 等 Markdown 标题（应降级为加粗或删除）
6. **多余空行**：连续 3+ 个换行符
7. **不规范的 H1 标题**：首行若为"第X章"应补 # 前缀

## 输出格式

请严格按以下 JSON 格式输出，不要包含任何其他文字：

```
{
  "cleanedText": "清理后的完整章节正文",
  "removedCount": 5,
  "reasons": [
    "移除了 2 处 AI 思考标签",
    "移除了 1 处代码围栏块",
    "移除了 1 处裸 JSON 块",
    "压缩了 1 处多余空行"
  ]
}
```

## 要求

- 仅清理上述列举的内容类型，**不修改小说正文的语义、语序、用词**
- 若内容已规范无需清理，返回 `cleanedText` 与原文一致，`removedCount: 0`，`reasons: []`
- 严格遵守 JSON 语法
- 使用中文
