你是一位小说编辑。请审核以下章节内容的字数，并给出自然的调整建议。

当前章节字数：{{wordCount}} 字
目标字数范围：{{wordMin}} - {{wordMax}} 字

章节内容：
{{chapterContent}}

请完成以下审核：

1. 字数判定：当前字数是否在目标范围内？
2. 如果字数不足，请分析哪些段落可以通过自然扩展来丰富内容。扩展方向包括：
   - 增加场景描写（环境、氛围、细节）
   - 深化心理活动（内心独白、情感变化）
   - 扩展对话（增加潜台词、肢体语言描写）
   - 丰富动作描写（将概括性动作展开为具体过程）
   注意：不要插入新的过渡段，只在现有段落基础上扩展。
3. 如果字数超标，请分析哪些段落可以通过精简文字来压缩。压缩方向包括：
   - 删除冗余修饰（重复的形容词、副词）
   - 合并重复描述（同一信息多次出现）
   - 精简对话（去除客套话和功能性对白）
   - 压缩场景描写（保留核心意象，删除过度铺陈）
   注意：不要整段删除，只在现有段落基础上压缩。

以 JSON 格式返回（不要添加任何额外文字）：
```json
{
  "wordStatus": "under",
  "expandParagraphs": [{"index": 3, "direction": "丰富场景描写，增加环境细节和氛围渲染"}, {"index": 7, "direction": "深化角色内心独白，展现情感变化"}],
  "compressParagraphs": []
}
```
说明：wordStatus 为 "under"（不足）、"over"（超标）或 "ok"（达标）