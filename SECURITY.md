# 安全政策

星月智能（Lunar Astral Agents）高度重视用户的安全与隐私。本文件说明了我们处理安全漏洞的流程、响应策略以及安全相关的最佳实践。

---

## 适用范围

本安全政策适用于星月智能平台的所有组件：

- **核心系统**: `lunar_astral`（月华）、`crystal_astral`（琉璃）
- **子系统**: 语音识别、语音合成、图像生成、代理服务、浏览器引擎等
- **前端资源**: 所有 `local_data/package/` 下的 Web 界面
- **构建脚本**: 各模块的 `build.ps1` 编译脚本

---

## 报告漏洞

### 报告渠道

如果你发现了安全漏洞，请**不要**通过公开 Issue 报告。请通过以下私密渠道联系我们：

- **GitHub Security Advisory**: 前往 [仓库安全页面](https://github.com/TayunStarry/Lunar_Astral_Agents/security/advisories/new)，点击 **"Report a vulnerability"** 提交私密报告
- **邮件报告**: 在项目仓库首页查看维护者邮箱，邮件标题请注明 `[安全漏洞]`

### 报告内容

请在报告中包含以下信息，以便我们快速定位和修复：

1. **漏洞概述**: 简要描述漏洞的性质和影响范围
2. **影响版本**: 受影响的版本号或提交哈希
3. **复现步骤**: 详细的复现步骤，包括环境配置
4. **潜在危害**: 该漏洞可能导致的后果（如数据泄露、远程代码执行等）
5. **修复建议**（可选）: 如有建议的修复方案，请一并提出
6. **联系方式**: 用于后续沟通的邮箱或联系方式

---

## 响应流程

| 阶段 | 预期时间 | 说明 |
|------|----------|------|
| 确认收到 | **2 个工作日内** | 维护者确认已收到漏洞报告，并分配追踪编号 |
| 初步评估 | **5 个工作日内** | 完成漏洞分类与严重性评估，通知报告者评估结论 |
| 修复开发 | 视严重程度而定 | **严重**: 72 小时内启动修复；**一般**: 14 天内启动 |
| 安全更新 | 修复完成后 | 发布新版本，并在 Release Notes 中说明安全更新内容 |
| 公开披露 | 修复发布后 | 发布安全公告，公开漏洞详情（经报告者同意后进行） |

### 漏洞严重性分级

| 级别 | 描述 | 示例 |
|------|------|------|
| **严重** | 可导致远程代码执行、认证绕过或敏感数据泄露 | RCE、任意文件读取、未授权 API 访问 |
| **高危** | 可导致部分功能绕过或有限数据泄露 | XSS 攻击、CSRF、路径遍历 |
| **中危** | 可导致服务降级或配置信息泄露 | 配置错误、日志泄露、弱加密 |
| **低危** | 理论上的安全风险或最佳实践偏离 | 缺少安全头、信息过度披露 |

### CVE 处理

- 对于严重或高危漏洞，维护者将协助报告者申请 CVE 编号
- CVE 编号将在安全公告中公开披露，同时致谢报告者
- 报告者可自行申请 CVE，也可由维护者代为申请

---

## AI 平台安全

星月智能作为 AI 推理平台，涉及模型加载、语音合成、图像生成等特有功能。以下是针对 AI 平台的安全建议：

### 模型文件安全

1. **来源验证**: 仅从可信来源下载 AI 模型文件（`.gguf`、`.safetensors`），避免使用未经验证的第三方模型
2. **完整性检查**: 下载模型后，验证文件哈希值是否与官方发布一致
3. **模型投毒防护**: 不要加载来源不明的模型文件，恶意模型可能导致推理结果异常或系统崩溃
4. **存储位置**: 模型文件应存储在受保护的目录中，避免被其他程序篡改

### 推理数据安全

1. **内存隔离**: 推理过程中的敏感数据（如用户对话、API 密钥）应在使用后及时清理，避免在内存中长期驻留
2. **提示注入防护**: 前端输入应经过严格校验和过滤，防止恶意提示注入攻击
3. **输出过滤**: AI 生成的内容（文本、图像、语音）应经过适当的安全过滤，避免生成有害内容
4. **会话隔离**: 不同用户的对话记录应严格隔离，避免数据泄露

---

## 通信与数据安全

### WebSocket / HTTP 通信

星月智能大量使用 WebSocket 和 HTTP 进行前后端通信：

1. **本地通信边界**: 平台默认仅在 `localhost` 监听，所有通信均为本地进程间通信。请勿修改为 `0.0.0.0` 绑定，避免暴露到公网
2. **消息校验**: WebSocket 消息和 HTTP 请求应经过严格校验，防止恶意输入
3. **文件上传安全**: 通过 `/file/write` 上传的文件应限制文件类型和大小，防止路径遍历攻击
4. **静态资源访问**: 通过 `/file/read/package/` 访问的静态资源应限制在项目目录内，防止目录遍历

### 本地数据存储

1. **数据库安全**: SQLite 数据库文件（`.db`）包含用户对话记录等敏感信息，应存储在受保护的目录中
2. **配置文件保护**: `lunar_config.json` 等配置文件可能包含 API 密钥等敏感信息，不要上传到公开仓库
3. **日志清理**: 生产日志中不得包含用户输入全文、Token 或认证凭据，定期清理过期日志
4. **备份安全**: 备份数据时应加密存储，避免备份文件泄露导致数据泄露

---

## 供应链安全

### 依赖审计

1. **Go 依赖**: 定期检查 `go.sum` 中的依赖是否存在已知漏洞，使用 `govulncheck` 进行自动化扫描
2. **Node.js 依赖**: 定期检查 `package-lock.json` 中的依赖，使用 `npm audit` 进行漏洞扫描
3. **C/C++ 依赖**: 项目使用了 ggml 等第三方 C/C++ 库，应关注上游安全公告，及时更新
4. **前端库**: marked.js、ECharts、highlight.js 等前端库应定期更新到最新稳定版

### 构建脚本安全

1. **脚本审查**: 各模块的 `build.ps1` 编译脚本应定期审查，确保未被篡改
2. **二进制分发**: 分发编译产物时，应提供哈希值供用户验证完整性
3. **签名验证**: 未来计划对发布版本进行数字签名，用户应验证签名后再运行

---

## 安全更新策略

### 版本支持

| 版本线 | 支持状态 | 说明 |
|--------|----------|------|
| 最新稳定版 | **完全支持** | 接收所有安全更新 |
| 上一个稳定版 | **有限支持** | 仅接收严重漏洞修复（最新版发布后 3 个月内） |
| 更早版本 | **不再支持** | 建议尽快升级 |

### 公告渠道

安全更新将通过以下渠道通知用户：

- GitHub Releases 页面
- README.md 中的重要通知区域
- 项目仓库的 Security Advisory 页面

---

## 安全最佳实践

### 对于用户

1. **及时更新**: 始终使用最新稳定版本，及时应用安全更新
2. **本地运行**: 星月智能设计为纯本地运行平台，请避免将服务暴露到公网
3. **模型安全**: 仅从可信来源下载 AI 模型文件，验证文件完整性
4. **端口控制**: 平台默认仅在 `localhost` 监听，请勿修改为 `0.0.0.0` 绑定
5. **配置文件保护**: 不要将 `lunar_config.json` 等包含敏感配置的文件上传到公开仓库

### 对于开发者

1. **依赖审计**: 定期检查依赖是否存在已知漏洞
2. **输入校验**: 所有外部输入（HTTP 请求、WebSocket 消息、文件上传）必须经过严格校验
3. **CGo 安全**: C/C++ 模块中注意缓冲区溢出、整数溢出等经典安全问题
4. **敏感信息**: 密钥、令牌等敏感信息不得硬编码，使用环境变量或配置文件管理
5. **日志安全**: 生产日志中不得包含用户输入全文、Token 或认证凭据

---

## 已知问题

当前**无**公开的已知安全漏洞。

---

## 漏洞赏金政策

星月智能目前**不提供**漏洞赏金或物质激励。我们感谢所有以负责任方式报告安全漏洞的研究者和用户，并会在安全公告中致谢（经报告者同意后）。

---

## 致谢

我们感谢所有为星月智能安全做出贡献的研究者和用户。您的负责任报告帮助我们构建了更安全的产品。

---

> 最后更新时间：2026 年 6 月
