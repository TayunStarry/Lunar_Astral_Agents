# 安全政策

## 前言

星月智能（Lunar Astral Agents）高度重视用户的安全与隐私。本文件说明了我们处理安全漏洞的流程、响应策略以及安全相关的最佳实践。

---

## 适用范围

本安全政策适用于星月智能平台的所有组件，包括但不限于：

- **核心系统**: `lunar_astral`（月华）、`crystal_astral`（琉璃）
- **子系统**: 语音识别、语音合成、图像生成、代理服务、浏览器引擎等
- **前端资源**: 所有 `local_data/package/` 下的 Web 界面
- **构建脚本**: 各模块的 `build.ps1` 编译脚本

---

## 报告漏洞

### 报告方式

如果你发现了安全漏洞，请**不要**通过公开 Issue 报告。请通过以下私密渠道联系我们：

- **邮件报告**: 见项目仓库首页联系信息（请在邮件标题注明 `[安全漏洞]`）
- **私密报告**: 在 GitHub 上使用 **"Report a vulnerability"** 功能（Security Advisory 私密报告）

### 报告内容

请在报告中包含以下信息，以便我们快速定位和修复：

1. **漏洞概述**: 简要描述漏洞的性质和影响范围
2. **影响版本**: 受影响的版本号或提交哈希
3. **复现步骤**: 详细的复现步骤，包括环境配置
4. **潜在危害**: 该漏洞可能导致的后果（如数据泄露、远程代码执行等）
5. **修复建议**（可选）: 如有建议的修复方案，请一并提出
6. **联系方式**: 用于后续沟通的邮箱或联系方式

---

## 响应流程

| 阶段 | 预期时间 | 说明 |
|------|----------|------|
| 确认收到 | **2 个工作日内** | 维护者确认已收到漏洞报告，并分配追踪编号 |
| 初步评估 | **5 个工作日内** | 完成漏洞分类与严重性评估，通知报告者评估结论 |
| 修复开发 | 视严重程度而定 | **严重**: 72 小时内启动修复；**一般**: 14 天内启动 |
| 安全更新 | 修复完成后 | 发布新版本，并在 Release Notes 中说明安全更新内容 |
| 公开披露 | 修复发布后 | 发布安全公告，公开漏洞详情（经报告者同意后进行） |

### 漏洞严重性分级

| 级别 | 描述 | 示例 |
|------|------|------|
| **严重** | 可导致远程代码执行、认证绕过或敏感数据泄露 | RCE、任意文件读取、未授权 API 访问 |
| **高危** | 可导致部分功能绕过或有限数据泄露 | XSS 攻击、CSRF、路径遍历 |
| **中危** | 可导致服务降级或配置信息泄露 | 配置错误、日志泄露、弱加密 |
| **低危** | 理论上的安全风险或最佳实践偏离 | 缺少安全头、信息过度披露 |

---

## 安全更新策略

### 版本支持

| 版本线 | 支持状态 | 说明 |
|--------|----------|------|
| 最新稳定版 | **完全支持** | 接收所有安全更新 |
| 上一个稳定版 | **有限支持** | 仅接收严重漏洞修复（最新版发布后 3 个月内） |
| 更早版本 | **不再支持** | 建议尽快升级 |

### 公告渠道

安全更新将通过以下渠道通知用户：

- GitHub Releases 页面
- README.md 中的重要通知区域
- 项目仓库的 Security Advisory 页面

---

## 安全最佳实践

### 对于用户

1. **及时更新**: 始终使用最新稳定版本，及时应用安全更新
2. **本地运行**: 星月智能设计为纯本地运行平台，请避免将服务暴露到公网
3. **模型安全**: 仅从可信来源下载 AI 模型文件（`.gguf`、`.safetensors`），验证文件完整性
4. **端口控制**: 平台默认仅在 `localhost` 监听，请勿修改为 `0.0.0.0` 绑定
5. **配置文件保护**: 不要将 `lunar_config.json` 等包含敏感配置的文件上传到公开仓库

### 对于开发者

1. **依赖审计**: 定期检查 `go.sum` / `package-lock.json` 中的依赖是否存在已知漏洞
2. **输入校验**: 所有外部输入（HTTP 请求、WebSocket 消息、文件上传）必须经过严格校验
3. **CGo 安全**: C/C++ 模块中注意缓冲区溢出、整数溢出等经典安全问题
4. **敏感信息**: 密钥、令牌等敏感信息不得硬编码，使用环境变量或配置文件管理
5. **日志安全**: 生产日志中不得包含用户输入全文、Token 或认证凭据

---

## 已知问题

当前**无**公开的已知安全漏洞。

---

## 致谢

我们感谢所有以负责任方式报告安全漏洞的研究者和用户。经报告者同意后，我们会在安全公告中列出贡献者名单。

---

> 最后更新时间：2026 年 6 月