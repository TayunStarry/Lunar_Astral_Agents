# Lunar 轻量级调度语言规范（v2.0）

## 1. 语言概述
Lunar 是一门面向流程调度与自动化的轻量脚本语言。只有一种数据类型——字符串，变量无需声明即全局可用。所有代码块并发启动，错误隔离。语法由 `@` 指令、`#` 变量、`*` 指针组成，可嵌入 Markdown 或 JSON 等宿主格式。

---

## 2. 词法约定
### 2.1 前缀符号
- `@` ：执行指令（如 `@run`、`@if`）
- `#` ：变量操作或变量引用
- `*` ：指针引用（代码库、快照）

### 2.2 字面量与标识符
- **字符串字面量**：使用单引号 `'...'` 或双引号 `"..."`。若字符串内需包含引号，可交替使用。
- **变量名**：`#` 后跟字母、数字、下划线，如 `#result`、`#file2`。
- **指针名**：`*` 后规则同上，如 `*backup`、`*snap1`。
- **变量引用**在表达式中写作 `#变量名`，求值时替换为变量值（空串若未定义，只写变量返回 `""`）。
- **数字**：仅作为字符串内容，无单独数字类型。

### 2.3 分隔与注释
- 每条指令独占一行。行首尾空白忽略。
- 以 `//` 开头的行是注释，解析器忽略。
- 空行忽略。

### 2.4 代码块定义
- **Markdown**：每一对 `'''lunar` 和 `'''` 间的行构成一个代码块。
  ```markdown
  '''lunar
  @run './task1'
  @wait #done
  '''
  ```
- **JSON/JavaScript**：最外层数组的每个元素是一个代码块，每个代码块是字符串数组，每一项为一行指令。
  ```json
  [
    ["@run './task1'", "@wait #done"],
    ["@log 'All tasks started'"]
  ]
  ```
  解释器按数组顺序加载各代码块，但**所有块在启动时同时开始并行执行**。

---

## 3. 变量与数据模型
### 3.1 全局变量
- 无需声明，首次赋值即创建。默认值为空字符串 `""`。
- 所有代码块共享同一张全局变量表，读写完全开放。

### 3.2 赋值与模式控制
变量赋值同时可以改变其读写模式。模式影响后续读取行为。

| 操作符 | 名称 | 行为 |
|--------|------|------|
| `-# 变量名 表达式` | 覆盖赋值 | 求值表达式，将结果**覆盖**写入变量，**并重置为普通读写模式**。 |
| `+# 变量名 表达式` | 追加赋值 | 求值表达式，将结果**追加**到当前值末尾（字符串拼接），**不改变**当前模式。 |
| `*# 变量名 表达式` | 只写赋值 | 赋值并设置变量为**只写模式**：任何读取（含表达式替换）都返回 `""`；内部值保留。 |
| `:# 变量名 表达式` | 只读赋值 | 赋值并设置变量为**只读模式**：后续任何试图修改该变量的指令（`-#`、`+#`、`*#`、`/#` 等）都将**被忽略**。 |
| `/# 变量名 表达式` | 解锁追加 | 解除只写或只读限制（恢复普通模式），同时将表达式结果**追加**到现有值末尾。 |

- 表达式中 `+` 表示字符串拼接，见 7.1。
- 注意：`-#` 会重置模式，因此 `*#` 或 `:#` 设置的读写保护会被后续 `-#` 覆盖。

**示例**：
```lunar
-# name 'Lunar'         // name = "Lunar", 普通模式
+# name ' Lang'         // name = "Lunar Lang"
*# secret 'key123'      // secret 只写，读取 #secret 得 ""
:# config 'port=80'     // config 只读
+# config 'port=81'     // 无效，config 仍为 "port=80"
/# secret ' backup'     // 解锁 secret，值变为 "key123 backup"，普通模式
```

### 3.3 特殊读取规则
- 若变量未定义，`#var` 返回 `""`。
- 若变量为只写模式，`#var` 返回 `""`。
- 以上规则适用于所有需要变量值的场景（表达式、指令参数等）。

---

## 4. 指针与快照
指针用于封装可复用的代码块或变量状态快照。

### 4.1 指针引用语法
- 读取：`*指针名` 作为变量（如 `@if '*ptr = "true"' ...`）：
  - 若该指针已定义 → 返回字符串 `"true"`。
  - 未定义 → 返回 `"false"`。
- 调用：将 `*指针名` 作为单独一行指令：
  - 若为 `class` 指针 → 创建新代码块并发执行。
  - 若为 `snapshot` 指针 → 将所有保存的变量及其模式恢复到当前全局环境（覆盖已有同名变量）。
  - 若未定义 → 视为空操作，跳过。

### 4.2 构建指针
- `@build *指针名 class`  
  将**当前代码块的所有指令行**（不含 `@build` 本身）保存为一个可重复执行的 `class` 指针。
- `@build *指针名 snapshot`  
  将**当前时刻所有已定义的变量**（包括值、读写模式）保存为 `snapshot` 指针。
- `@end *指针名`  
  自动保存一个 `snapshot` 指针，然后**清空所有全局变量**（全部置为未定义）。

---

## 5. 执行模型与并发
### 5.1 并行启动，错误隔离
- 所有代码块在启动时同时开始执行，块内从上到下行顺序执行。
- 任何指令的运行时错误（如 `@run` 调用失败、文件不存在）仅终止当前代码块，不影响其他块继续运行。
- 语法错误的行在加载时会被忽略（或记录到错误流），不会阻止其他正确行/块的执行。

### 5.2 阻塞指令
以下指令会阻塞**当前代码块**，但其他块不受影响：
- `@catch` （等待输出行）
- `@wait` （等待变量非空）
- `@sleep` （纯等待）
- `@run` （未包装 `@catch` 时等待进程结束）

---

## 6. 内置指令参考
指令通用格式：`@指令名 参数...` ，参数以空格分隔。含空格参数须用引号包裹。

### 6.1 程序调用与捕获
#### @run — 执行外部程序
```
@run 路径 [参数1 [参数2 ...]]
```
- `路径`：可执行文件路径（字符串字面量或 `#变量`）。
- `参数`：传递给程序的命令行参数，顺序拼接。每个参数都会作为独立 argv 元素，若包含空格需用引号。
- 阻塞当前块直到进程退出。进程的标准输出和标准错误合并到内部缓冲区，供 `@catch` 使用。
- 示例：
  ```lunar
  -# tool './scripts/backup.exe'
  @run #tool '--path' '/data' '--verbose'
  ```

#### @catch — 捕获输出中的特定行
```
@catch #变量 '目标子串'
@run ...                  // 紧接着的下一行必须是 @run
```
- 包装紧接的 `@run`，捕获其输出流（stdout+stderr）。阻塞直到出现**包含“目标子串”的一行**，将该**完整行**以**追加模式（`+#`）** 写入指定变量，然后解除阻塞，`@run` 继续运行至结束。
- 若 `@catch` 下一行不是 `@run`，则本指令无效（视为空操作）。
- 若直到进程结束仍未出现目标子串，不写入任何内容，代码块继续。
- 示例：
  ```lunar
  @catch #log 'SUCCESS'
  @run './backup.exe'
  // 运行中一旦某行输出含 SUCCESS，那一行追加入 #log
  ```

### 6.2 变量处理
#### @filter — 正则提取 / 过滤
```
@filter #变量 '正则表达式'
```
- 对变量的当前值应用正则表达式，将**匹配结果**以**覆盖模式（`-#`）** 写入变量（会重置为普通模式）。
- 支持模板关键字，需用大写：
  - `URL` ：提取字符串中第一个 http/https URL。
  - `VALUE`：提取所有数字序列，以空格连接。
- 示例：
  ```lunar
  -# html '<a href="https://a.com">A</a> <b>12 34</b>'
  @filter #html 'URL'          // html = "https://a.com"
  @filter #html 'VALUE'        // html = "12 34"
  ```

#### @math — 数学运算
```
@math #变量 '算术表达式'
```
- 将算术表达式求值，结果以**字符串**形式**覆盖（`-#`）** 写入变量（重置为普通模式）。
- 表达式中变量引用会自动转换为数字：非数字字符串视为 `0`。
- 支持运算符：`+` `-` `*` `/` `%` `()` ，内置函数 `int(expr)`（向下取整）。
- 示例：
  ```lunar
  @math #x '#x + 1'          // x 从 "" -> 0 -> "1"
  @math #y 'int(#x) * 3'     // y = "3"
  ```

#### @wait — 等待变量非空
```
@wait #变量
```
- 阻塞当前块直至指定变量值不为 `""` （读取时遵循只写规则：若变量只写则永远返回 `""`，会死锁，应避免）。

### 6.3 控制流
#### @cycle — 条件循环
```
@cycle '条件表达式' '循环体指令串'
```
- 重复执行“循环体指令串”，直到“条件表达式”为真（值非空且非 `"false"`）。
- 条件、循环体均可以是单条指令或多条指令的分号串 `';'`，也可引用变量（变量内容被当作指令串执行）。
- 示例：
  ```lunar
  -# i '0'
  @cycle '#i != "10"' '@math #i "#i + 1"; @log #i'
  ```
  或使用指针：
  ```lunar
  -# i '0'
  @build *incLoop class
  // *incLoop 代码块内容为下面两行
  @math #i '#i + 1'
  @log #i

  @cycle '#i != "10"' '*incLoop'
  ```

#### @if — 条件分支
```
@if '条件' ? '真分支指令' : '假分支指令'
```
- 若条件为真，执行 `?` 后的指令串；否则执行 `:` 后的指令串。
- 分支指令可以是单条指令或多条用 `;` 分隔。
- 示例：
  ```lunar
  @if '#score > "60"' ? '@log "Pass"' : '@log "Fail"'
  ```

#### @retry — 错误重试
```
@retry 冷却毫秒 最大重试次数 #错误变量
```
- 当**当前代码块**发生任何运行时错误（如 `@run` 返回非零、文件操作失败、指令参数无效等），等待冷却毫秒数后，**重新从头执行整个代码块**（变量表不重置，保持全局状态）。
- 错误信息（字符串描述）以**追加模式**写入 `#错误变量`。
- 达到最大重试次数后若仍失败，代码块终止。最大重试次数应为大于 0 的整数；若 `<1` 则视为不重试。
- 示例：
  ```lunar
  @retry 1000 3 #err
  @run './unstable.exe'
  ```

### 6.4 文件与输出
#### @write — 写入文件
```
@write #路径变量 #内容变量
```
- 将 `#内容变量` 的值**覆盖写入**到 `#路径变量` 指定的文件（文件不存在则创建）。
- 遵循变量模式：若内容变量为只写，写入空串；若路径变量为空或只写，操作失败并报错。
- 写入采用 UTF-8 编码。

#### @read — 读取文件
```
@read #路径变量 #目标变量
```
- 读取 `#路径变量` 指定文件的全部文本内容，以**覆盖模式（`-#`）** 写入 `#目标变量`（重置为普通模式）。
- 若文件不存在或读取失败，`#目标变量` 被赋值为空字符串，并产生运行时错误（可被 `@retry` 捕获）。

#### @log — 输出日志
```
@log '内容' 或 @log #变量
```
- 将参数内容输出到解释器的标准输出，末尾自动添加换行。支持变量替换。
- 若变量只写，输出空行。

### 6.5 其他
#### @web — 打开浏览器
```
@web #变量
```
- 调用操作系统默认浏览器打开变量中的 URL。若变量为空或无效，忽略。

#### @sleep — 休眠
```
@sleep 毫秒数
```
- 阻塞当前代码块指定毫秒。参数可以是数字字面量或变量（非数字视为 0）。

---

## 7. 表达式语法
### 7.1 赋值表达式中的字符串拼接
在 `-#`、`+#`、`*#`、`:#`、`/#` 操作符的表达式部分，支持用 `+` 连接字符串：
```
-# message 'Hello ' + #name + '!'
```
- 求值时从左到右依次拼接，无优先级概念。若要改变顺序，可使用变量作中间存储。
- 若变量只写，其引用部分视作空串。

### 7.2 条件表达式
用于 `@if` 和 `@cycle`，结果必须能判断真假（空字符串或 `"false"` 视为假，其余视为真）。
- 比较运算符：`=` （等于）、`!=`、`>`、`<`、`>=`、`<=`。按字符串字典序比较（若需数值比较，先使用 `@math` 格式化数字）。
- 逻辑运算符：`&` （与）、`|` （或）、`!` （非）。支持括号 `()` 改变优先级。
- 示例：`'(#score > "80") & (#level != "0")'`。

---

## 8. 嵌入宿主格式
解释器应提供以下接口：
- `lunar.loadMarkdown(mdText)`：扫描 ```lunar 代码块，每一块为一个独立代码块。
- `lunar.loadJSON(jsonArray)`：接收 `string[][]`，每个子数组为一个代码块。
- 所有代码块加载完毕后统一启动执行（并发）。

---

## 9. 完整示例
```lunar
'''lunar
-# target '/data'
-# tool './backup.exe'
-# log ''
-# retryErr ''

@retry 2000 2 #retryErr
@catch #log 'OK'
@run #tool '--path' #target

@wait #log
@if '#log != ""' ? (@log 'Backup success'; @web 'http://monitor/ok') : @log 'Backup failed'
'''

// 并发写入一个状态文件
'''lunar
@wait #log
-# statusFile './last_status.txt'
-# msg 'last backup: ' + #log
@write #statusFile #msg
@sleep 1000
@read #statusFile #readback
@log #readback
'''
```

---

## 10. 一致性要求与实现建议
- **原子性**：解释器必须保证单条指令对变量的读写具有原子性，避免多块并发时内部状态撕裂。
- **快照语义**：`@build snapshot` 和 `@end` 必须保存变量的值与模式；恢复时完整复原模式（包括只写、只读）。
- **错误处理**：运行时错误统一记录，不崩溃解释器。语法错误的行在解析阶段丢弃并向 stderr 输出警告。
- **扩展性**：指令集可通过插件注册，但本规范的 @ 指令必须保留行为一致性。